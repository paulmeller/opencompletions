This is a sample project for running Claude Code inside of Replit.

All packages are installed, all that's required is:
- Set `CLAUDE_CONFIG_DIR` to `/home/runner/workspace/.claude-user`

The `.claude-user` directory is ignored by Git, however it is VERY IMPORTANT you do not share this directory: it contains sensitive data and credentials.